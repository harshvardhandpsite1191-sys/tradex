# AI-QROS Backend — Requirements
# Phase 0: Project Foundation

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0              # async PostgreSQL driver
psycopg2-binary==2.9.9       # sync PostgreSQL driver (for Alembic)

# --- Redis / Celery ---
celery==5.4.0
redis==5.0.4
flower==2.0.1                # Celery monitoring UI

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Validation & Settings ---
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# --- HTTP Client ---
httpx==0.27.0
aiohttp==3.9.5

# --- Data & ML (Phase 1 knowledge base seeding) ---
pandas==2.2.2
numpy==1.26.4

# --- ML Models (Phase 16) ---
xgboost==2.0.3
lightgbm==4.3.0
catboost==1.2.5
scikit-learn==1.4.2

# --- Explainability (Phase 16) ---
shap==0.45.1

# --- Drift Detection (Phase 22) ---
evidently==0.4.30

# ── Data Providers ───────────────────────────────────────────
upstox-python-sdk>=2.3.0       # Upstox: live option chain, tick data, live snapshots
yfinance>=0.2.40               # Global markets daily OHLCV (S&P500, Nikkei, Brent, etc.)
smartapi-python>=1.3.6         # Angel One SmartAPI: historical F&O candle data (free)
pyotp>=2.9.0                   # TOTP for Angel One 2FA auth
requests>=2.31.0               # HTTP client for NSE Bhavcopy downloads
APScheduler>=3.10.4            # In-process scheduler (replaces separate Celery Beat process)

# --- Experiment Tracking (Phase 0 — MLflow) ---
mlflow==2.13.0

# --- Logging & Monitoring ---
structlog==24.1.0
prometheus-fastapi-instrumentator==7.0.0

# --- Utilities ---
python-dateutil==2.9.0
pytz==2024.1
uuid==1.30
